language: php
php:
 - 7.2.10

before-install:
  - chmod +x build.sh
  - composer install --no-interaction
  - composer update

install:
  - php bin/console app:install

script:
  - vendor/bin/phpunit  
